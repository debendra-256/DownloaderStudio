[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"

# SPA fallback - redirect all routes to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
